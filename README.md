use requirements.txt to install all libraries (use this command in your terminal "pip install -r requirements.txt")
note: you must open terminal from the same file path that the files you download it 
